# Optional absolute path to a local checkout containing skills/<slug>/SKILL.md.
# Leave empty to use the checked-in public-safe snapshot.
SKILLS_REPO_PATH=

# Optional port for the single Node process.
PORT=4173

# Optional network bind. The safe local default is used when omitted.
HOST=127.0.0.1
